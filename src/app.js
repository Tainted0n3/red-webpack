const greeting = 'Ragged Edge Development - Webpack and Babel';

const appElement = document.getElementById('app');
const header = document.createElement('h1');
header.innerText = greeting;
appElement.appendChild(header);
